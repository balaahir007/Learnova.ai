services:
  - type: web
    name: Learnova.ai
    env: static
    buildCommand: npm run build
    staticPublishPath: dist
    routes:
      - type: redirect
        source: /
        destination: /index.html
        status: 200
      - type: rewrite
        source: /*
        destination: /index.html
